# How to use seeder
1. Configure seed_dev.json file under config folder. Make sure it contains the DBHost url that points to the new db you wish to setup
2. Run coffee ./seeder.coffee to populate new data